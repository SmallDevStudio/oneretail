import { useState } from "react";
import { AppLayout } from "@/themes";

const HousingLoad = () => {
    return (
        <div>
            CYC
        </div>
    )
}

export default HousingLoad;

HousingLoad.getLayout = (page) => <AppLayout>{page}</AppLayout>
HousingLoad.auth = true